diff --git a/setup.py b/setup.py
index 3c5fe7c..dd59149 100644
--- a/setup.py
+++ b/setup.py
@@ -39,13 +39,11 @@ def parse_version_from(path):
 
 
 # for the binary wheel
-libdir = os.path.realpath("install/lib")
-incdir = os.path.realpath("install/include")
+libdir = os.path.realpath(os.environ["WASM_LIBRARY_DIR"] + "/lib")
+incdir = os.path.realpath(os.environ["WASM_LIBRARY_DIR"] + "/include")
 libs = ["eccodes"]
 
-if "--binary-wheel" in sys.argv:
-    sys.argv.remove("--binary-wheel")
-
+if True:
     # https://setuptools.pypa.io/en/latest/userguide/ext_modules.html
     ext_modules = [
         setuptools.Extension(
