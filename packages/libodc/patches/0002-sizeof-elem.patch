diff --git a/src/odc/codec/Integer.h b/src/odc/codec/Integer.h
index 683f172..36de4d1 100644
--- a/src/odc/codec/Integer.h
+++ b/src/odc/codec/Integer.h
@@ -106,7 +106,7 @@ private:  // methods
     }
 
     void decode(double* out) override {
-        static_assert(sizeof(ValueType) == sizeof(out), "unsafe casting check");
+        static_assert(sizeof(ValueType) == sizeof(*out), "unsafe casting check");
 
         ValueType* val_out = reinterpret_cast<ValueType*>(out);
         InternalValueType s;
@@ -143,7 +143,7 @@ private:  // methods
     }
 
     void decode(double* out) override {
-        static_assert(sizeof(ValueType) == sizeof(out), "unsafe casting check");
+        static_assert(sizeof(ValueType) == sizeof(*out), "unsafe casting check");
 
         ValueType* val_out = reinterpret_cast<ValueType*>(out);
         InternalValueType s;
diff --git a/src/odc/codec/IntegerMissing.h b/src/odc/codec/IntegerMissing.h
index b6312d1..cb97f77 100644
--- a/src/odc/codec/IntegerMissing.h
+++ b/src/odc/codec/IntegerMissing.h
@@ -53,7 +53,7 @@ private:  // methods
     }
 
     void decode(double* out) override {
-        static_assert(sizeof(ValueType) == sizeof(out), "unsafe casting check");
+        static_assert(sizeof(ValueType) == sizeof(*out), "unsafe casting check");
 
         ValueType* val_out = reinterpret_cast<ValueType*>(out);
         InternalValueType s;
